# Contoh saja — JANGAN commit secret asli ke repo.
# Cara pakai lokal: copy jadi secret.yaml (sudah di-.gitignore), isi nilai asli, lalu:
#   kubectl apply -f k8s/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: projectpulse-secret
type: Opaque
stringData:
  DB_USERNAME: postgres
  DB_PASSWORD: change-me
  JWT_SECRET: change-me
  LLM_API_KEY: change-me
