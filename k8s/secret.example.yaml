# Example only. Copy to secret.yaml, replace every change-me value, and never commit it.
apiVersion: v1
kind: Secret
metadata:
  name: projectpulse-secret
  namespace: projectpulse
type: Opaque
stringData:
  APP_KEY: "change-me-run-php-artisan-key-generate-show"
  DB_USERNAME: "projectpulse"
  DB_PASSWORD: "change-me"
  OPENAI_API_KEY: ""
  GEMINI_API_KEY: ""
