# TODO: ganti isi Dockerfile ini sesuai stack backend yang dipilih (Laravel atau Next.js).
# Contoh referensi tersedia di docs/dockerfile-examples/:
#   - Dockerfile.laravel.example
#   - Dockerfile.nextjs.example
#
# Ketentuan wajib (lihat root README.md):
#   - Image harus bisa dibangun ulang dari source (`docker build .`) tanpa langkah manual tambahan.
#   - Jangan hardcode secret/API key di sini — gunakan environment variable saat runtime.
